# Site — vuetify-dynamic-fields docs

This is a small Vite + Vue 3 + Vuetify docs site for the `vuetify-dynamic-fields` plugin.

Quick start:

```bash
cd site
npm install
npm run dev
```

The site registers the plugin and shows a live example using the plugin's `DynamicFieldsGenerator` component.
